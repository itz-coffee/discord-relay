This is a helix compatible branch, same instructions as the main branch but move the plugins folder to your schema instead.
Rename and move the `discord_relay.yml.example` to your helix gamemode folder